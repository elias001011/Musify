[build]
  command = "if [ ! -d 'flutter' ]; then git clone https://github.com/flutter/flutter.git -b stable; fi && ./flutter/bin/flutter build web --release --web-renderer=html"
  publish = "build/web"

[build.environment]
  FLUTTER_VERSION = "stable"
